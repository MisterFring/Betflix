{% extends "/base.html.twig" %}

{% block myBets %}
		<table class="table table-bordered" style="width: 70%" align="center">
			<thead class="thead-dark">
				<tr>
					<th>Odd</th>
                    <th style="text-align: right">Date</th>
					<th style="text-align: right">Potential Gain</th>
					<th style="text-align: right">Bet Details</th>
				</tr>
			</thead>
			{% for bet in bets %}
				<tr style="background-color: {{ bet.color }}">
					<td>
                    <span>Odd : </span>
                    <span class="font-weight-bold">{{ bet.oddsTotal }}</span>
				    /
                    <span>Amount : </span>
                    <span class="font-weight-bold">{{ bet.betAmount }}</span>
					</td>
                    <td style="text-align: right">
                        {{ bet.betDate }}
                    </td>
					<td style="text-align: right">
						<span style="color: #666666;">{{ (bet.betAmount *  bet.oddsTotal)|round(2) }}€</span>
					</td>
					<td align="right" style="background-color: white">
						<div class="btn-group dropleft">
							<button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								Details
							</button>
							<div class="dropdown-menu">
								{% for item in bet.details %}
									<a class="dropdown-item" href="#">
									<span class="team">{{ item.match }} - {{ item.odd }}</span>
									<span style="font-weight: lighter;color: #666666;">[{{ item.choice }}]</a>
								{% endfor %}
							</div>
						</div>
					</td>
				</tr>
			{% endfor %}
		</table>
	</div>

{% endblock %}
